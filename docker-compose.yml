version: "3.9"

services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      JWT_SECRET: change-me
      JWT_EXPIRES_IN: 1d
      DB_STORAGE: /app/database.sqlite
    volumes:
      - ./database.sqlite:/app/database.sqlite
